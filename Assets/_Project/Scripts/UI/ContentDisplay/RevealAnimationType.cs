namespace InkEcho.UI.ContentDisplay
{
    /// <summary>
    /// Enum các kiểu animation cho reveal
    /// </summary>
    public enum RevealAnimationType
    {
        FadeIn,      // Fade in từ alpha 0 → 1
        SlideInUp,   // Slide từ dưới lên
        ScaleIn,     // Scale từ 0 → 1
        None         // Không animation
    }
}
